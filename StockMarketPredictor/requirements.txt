# --- API & serving (Phase 5) ------------------------------------------------
fastapi==0.141.1
uvicorn[standard]==0.46.0
pydantic==2.13.3
apscheduler==3.11.3     # in-process background scheduler for live news ingestion

# --- NLP metadata router (Phase 1) ------------------------------------------
tensorflow-cpu==2.21.0   # inference-only serving image; swap for `tensorflow`
                         # if you need GPU support elsewhere in your stack
nltk==3.10.3
scikit-learn==1.8.0      # unpickles the fitted BoW vectorizer + label encoder

# --- Vector database & agentic orchestration (Phases 2 & 4) ----------------
langchain==1.3.15
langchain-core==1.5.5
langchain-google-genai==4.3.4
langchain-community==0.4.2
langgraph==1.2.11
chromadb==1.5.9

# --- Quantitative baseline (Phase 3) ----------------------------------------
xgboost==3.2.0
yfinance==1.6.0
pandas==3.0.2
numpy==2.4.4
